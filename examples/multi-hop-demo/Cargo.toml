[package]
name = "multi-hop-demo"
version.workspace = true
edition.workspace = true

[dependencies]
cortex-core = { path = "../../crates/core" }
cortex-signal = { path = "../../crates/signal" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
